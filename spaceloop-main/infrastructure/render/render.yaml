services:
  - type: web
    name: spaceloop-platform
    env: python
    buildCommand: pip install -r requirements.txt || pip install Flask Flask-SQLAlchemy requests
    startCommand: python3 app.py
    envVars:
      - key: PORT
        value: 5001
      - key: SECRET_KEY
        generateValue: true
