# API Authentication & Authorization
- Seekers and Hosts are authenticated via session tokens and API headers.
- Role-based authorization (`seeker`, `owner`, `admin`).
- Rate limiting: 20 requests/minute per client IP for AI routes.
