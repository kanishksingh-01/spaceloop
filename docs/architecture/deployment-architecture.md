# Deployment Architecture
- Dockerized multi-stage container deployment.
- Edge delivery via Cloudflare Tunnel or Nginx reverse proxy with SSL termination.
- Native Render blueprint (`infrastructure/render/render.yaml`).
