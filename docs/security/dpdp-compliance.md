# DPDP Act 2023 Section 8 Compliance
SpaceLoop never stores 12-digit Aadhaar numbers or biometrics. Only masked displays (`XXXX-XXXX-4821`) and salted SHA-256 hashes (`sha256(aadhaar + salt)`) are persisted.
