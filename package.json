{
  "name": "spaceloop",
  "version": "2.5.0",
  "private": true,
  "description": "SpaceLoop — Flexible Peer-to-Peer Physical Space Marketplace",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "dev": "cd frontend && npm run dev"
  }
}
